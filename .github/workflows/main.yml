name: Main Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      # Your existing build steps go here

      - name: Trigger Workflow in Repository 2
        run: gh workflow run Triggered_Workflow.yml -R sreeharsha-alluri/GitHub_Action_Repo2 -F trigger_automatically=true
